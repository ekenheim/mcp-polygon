# Polygon.io API Key - Get yours at https://polygon.io/
POLYGON_API_KEY=your_polygon_api_key_here

# Optional: Set to enable debug logging
DEBUG=false

# Optional: Set custom port (default is 8000)
PORT=8000

# Optional: Enable HTTP transport for Docker (default: true in Docker)
MCP_HTTP_TRANSPORT=true
